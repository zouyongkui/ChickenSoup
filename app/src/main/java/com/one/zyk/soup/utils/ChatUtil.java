package com.one.zyk.soup.utils;


/**
 * Author ：Yongkui.Zou
 * Date : 2018/2/9 21:35
 * Company ：北京虹宇科技有限公司
 * Function
 */
public class ChatUtil {

//    public void sendText(String content, String id, boolean isGroup) {
//        //创建一条文本消息，content为消息文字内容，toChatUsername为对方用户或者群聊的id，后文皆是如此
//        EMMessage message = EMMessage.createTxtSendMessage(content, id);
//        //如果是群聊，设置chattype，默认是单聊
//        if (isGroup) {
//            message.setChatType(EMMessage.ChatType.GroupChat);
//        }
//        //发送消息
//        EMClient.getInstance().chatManager().sendMessage(message);
//    }
//
//    public void sendAudio(String filePath, int length, String id, boolean isGroup) {
//        //filePath为语音文件路径，length为录音时间(秒)
//        EMMessage message = EMMessage.createVoiceSendMessage(filePath, length, id);
//        //如果是群聊，设置chattype，默认是单聊
//        if (isGroup) {
//            message.setChatType(EMMessage.ChatType.GroupChat);
//        }
//        EMClient.getInstance().chatManager().sendMessage(message);
//    }
//
//    public void sendPic(String imagePath, String id, boolean isGroup, boolean isShrink) {
//        //imagePath为图片本地路径，false为不发送原图（默认超过100k的图片会压缩后发给对方），需要发送原图传true
//        EMMessage message = EMMessage.createImageSendMessage(imagePath, isShrink, id);
//        //如果是群聊，设置chattype，默认是单聊
//        if (isGroup) {
//            message.setChatType(EMMessage.ChatType.GroupChat);
//        }
//        EMClient.getInstance().chatManager().sendMessage(message);
//    }

}
