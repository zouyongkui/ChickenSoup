<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.one.zyk.chickensoup">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".SoupApp"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.test" />
        <activity android:name=".activity.WebActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:sharedUserId="android.uid.system" />
        <activity android:name=".activity.CommentsListActivity" />
        <activity android:name=".activity.ManagementActivity" />
        <activity android:name=".activity.CommentManagementActivity" />
        <activity android:name=".activity.TabActivity">

        </activity>
        <activity
            android:name=".activity.PostCommentActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
    </application>

</manifest>